# 빌드노트 — AI-Morning-Brief

## FEAT-01 — 프로젝트 골격 + 설정로더 + CLI 스켈레톤
- 처리 이슈: #1
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `requirements.txt` | 의존성 (feedparser, requests, PyYAML, anthropic, python-dateutil) |
| `.gitignore` | data/, reports/, .env, config/sources.yaml, __pycache__/ 제외 |
| `main.py` | argparse CLI — 7개 옵션 + pipeline.run() 배선 (ImportError 우아 처리) |
| `src/__init__.py` | src 패키지 init |
| `src/config_loader.py` | load_config / load_sources / load_secrets / load_operator_profile / DEFAULT_PROFILE |
| `config/config.yaml` | 설정 파일 (paths + env override 주석, pipeline/claude/discord 섹션) |
| `config/sources.example.yaml` | 소스 예시 5개 (enabled 필드 포함) |
| `config/operator_profile.md` | 운영자 프로필 템플릿 |
| `config/.env.example` | 비밀값 예시 (ANTHROPIC_API_KEY, DISCORD_WEBHOOK_URL, 경로 override) |
| `data/.gitkeep` | data/ 폴더 추적용 |
| `reports/.gitkeep` | reports/ 폴더 추적용 |
| `tests/fixtures/.gitkeep` | tests/fixtures/ 폴더 추적용 |
| `scripts/.gitkeep` | scripts/ 폴더 추적용 |

### 수동 검증 방법 (Bash 차단 환경용)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
# 1. 의존성 설치
pip install -r requirements.txt

# 2. --help: 7개 옵션 출력 확인
python main.py --help

# 3. --test: ImportError 없이 종료코드 0 확인
python main.py --test
echo "종료코드: $?"

# 4. load_config() + env override 확인
python -c "
from src.config_loader import load_config
cfg = load_config()
print('type:', type(cfg))
print('paths:', cfg.get('paths'))
"

MORNINGBRIEF_DB=/tmp/x.db python -c "
from src.config_loader import load_config
cfg = load_config()
print(cfg['paths']['db'])  # 기대: /tmp/x.db
"

# 5. load_sources() — sources.yaml 없음 → example fallback + WARNING
python -c "
import logging; logging.basicConfig(level=logging.WARNING)
from src.config_loader import load_sources
srcs = load_sources()
print('소스 수:', len(srcs))
print('첫 소스:', srcs[0] if srcs else '없음')
"

# 6. load_operator_profile() — 파일 있을 때 is_default=False
python -c "
from src.config_loader import load_operator_profile
text, is_default = load_operator_profile()
print('is_default:', is_default)
"

# 7. operator_profile 부재 시 DEFAULT_PROFILE + WARNING
python -c "
import logging; logging.basicConfig(level=logging.WARNING)
from src.config_loader import load_operator_profile
text, is_default = load_operator_profile('nonexistent.md')
print('is_default:', is_default)  # 기대: True
"

# 8. load_secrets()
python -c "
from src.config_loader import load_secrets
s = load_secrets()
print('keys:', list(s.keys()))
"
```

### 구현 판단 / 특이사항
- `load_sources`의 `example_path` 계산 시 `os.path.dirname(path)`를 사용해 사용자 지정 path에서도 올바른 경로를 참조하도록 구현 (FEAT 문서의 하드코딩 방식 대비 안정성 개선, 동작은 동일)
- `main.py`의 `sys.exit(main())` 패턴으로 종료코드 0/비0을 명시적으로 처리

### 수정 내역 (2026-06-21 — 설계 계약 재확인)
`main.py` help/epilog 문자열 2개를 설계 계약에 맞게 수정 (동작 로직 무변경):
- `--dry-run` help: "리포트를 생성하되 Discord 전송 등 외부 출력을 건너뜀" → "실제 저장/전송 없이 실행 계획과 리포트 미리보기만 출력 (DB·raw·리포트·Discord 무변경)"
- `--force-fallback` help: "sources.yaml이 있어도 sources.example.yaml을 강제 사용" → "Claude 분석을 강제로 fallback(기본 요약) 모드로 실행 (Claude 연동 실패 검증용)"
- 두 옵션의 epilog 주석도 동일 방향으로 수정

### 범위 준수 확인
- 수집/저장/분석/리포트/전송/진단 로직 없음 (FEAT-02~07 미선구현)
- `config/sources.yaml` 실파일 없음 (.gitignore에 포함)
- 비밀값 하드코딩 없음 (모두 .env/환경변수 경유)

### 제안 commit 메시지
```
feat(FEAT-01): 프로젝트 골격 + 설정로더 + CLI 스켈레톤

- 05-개발/ 폴더 구조 생성 (src/, config/, data/, reports/, tests/fixtures/, scripts/)
- requirements.txt, .gitignore 추가
- src/config_loader.py: load_config(env override), load_sources(fallback),
  load_secrets(.env 파싱), load_operator_profile(DEFAULT_PROFILE 폴백)
- main.py: argparse 7개 옵션 + pipeline.run() 배선 (ImportError 우아 처리)
- config/config.yaml, sources.example.yaml, operator_profile.md, .env.example 추가

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-02 — 저장 계층 (SQLite)
- 처리 이슈: #2
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 추가 문서 참조 이유
설계서 3장을 콕 집어 읽음 — FEAT-02 문서의 SCHEMA 플레이스홀더가 "설계서 3장 DDL 전체"를 가리켜 정확한 컬럼 목록·인덱스명 확인이 필요했음.

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/storage.py` | SQLite 저장 계층 (신규) |

### 구현 내용
- `SCHEMA` 상수: 설계서 3장 DDL 그대로 — articles(url PK, title, published_at, source, collected_at, raw_excerpt, summary, tags, importance, relevance, analyzed) + idx_articles_published + idx_articles_collected + meta(key PK, value)
- `init_db(db_path)`: 부모 디렉토리 생성 → sqlite3.connect → row_factory=Row → executescript(SCHEMA) → commit → conn 반환
- `insert_article(conn, a)`: INSERT OR IGNORE → rowcount==1이면 True(신규), 0이면 False(중복). DB 오류는 예외 그대로 올림.
- `update_analysis(conn, url, summary, tags, importance, relevance, analyzed)`: UPDATE articles SET … WHERE url=?. tags list → json.dumps.
- `get_articles_by_range(conn, start, end)`: collected_at BETWEEN → ORDER BY importance DESC, published_at DESC → dict 리스트(tags는 json.loads, None/빈 값은 []).
- `get_meta(conn, key, default=None)`: SELECT value FROM meta WHERE key=? → row["value"] or default.
- `set_meta(conn, key, value)`: INSERT … ON CONFLICT(key) DO UPDATE SET value=excluded.value.

### 수동 검증 방법 (Bash 차단 환경용)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
python -c "
from src.storage import init_db, insert_article, update_analysis, get_articles_by_range, get_meta, set_meta

# 1. init_db: 테이블/인덱스 생성 확인
c = init_db('data/test_feat02.db')
tables = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\").fetchall()]
indexes = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='index' ORDER BY name\").fetchall()]
print('테이블:', tables)     # ['articles', 'meta']
print('인덱스:', indexes)    # ['idx_articles_collected', 'idx_articles_published']

# 2. insert_article: 첫 True, 두 번째 False, 1행만 존재
a = {'url':'https://example.com/a1','title':'테스트','published_at':'2026-06-21T00:00:00','source':'ex','collected_at':'2026-06-21T01:00:00','raw_excerpt':'본문'}
r1 = insert_article(c, a)
r2 = insert_article(c, a)
count = c.execute('SELECT COUNT(*) FROM articles').fetchone()[0]
print(f'insert: {r1} {r2}, 행수: {count}')   # True False, 행수: 1

# 3. update_analysis
update_analysis(c, a['url'], '요약', ['ai'], 4, 3, 1)
row = dict(c.execute('SELECT * FROM articles').fetchone())
print('tags 저장값:', row['tags'])             # '[\\"ai\\"]'

# 4. set_meta / get_meta 왕복
set_meta(c, 'last_success_at', '2026-06-20T04:30:00')
print('get_meta:', get_meta(c, 'last_success_at'))   # 2026-06-20T04:30:00
print('없는키:', get_meta(c, 'x', 'default'))         # default

# 5. get_articles_by_range: dict list, tags는 list
arts = get_articles_by_range(c, '2026-06-01T00:00:00', '2026-06-30T23:59:59')
print('건수:', len(arts), '/ tags 타입:', type(arts[0]['tags']))  # 1 / <class 'list'>

c.close()
import os; os.remove('data/test_feat02.db')
print('--- 완료 조건 전부 통과 ---')
"
```

### 구현 판단 / 특이사항
- `tags` None/빈 문자열 모두 빈 리스트로 정규화 (json.loads 실패 시도 포함).
- `get_articles_by_range`는 설계서 동작 흐름대로 collected_at 기준 BETWEEN 조회.
- DB 쓰기 오류는 예외를 그대로 올려 pipeline 종료코드 1 + last_success_at 미갱신(catch-up 유도) — 별도 try/except 없음.
- ORM 없음, 표준 sqlite3만 사용.

### 범위 준수 확인
- 수집(FEAT-03)/분석(FEAT-04)/리포트(FEAT-05)/전송(FEAT-06)/파이프라인(FEAT-07) 선구현 없음.
- 보관 기간 삭제 로직 없음(V0.1 전량 보관).

### 제안 commit 메시지
```
feat(FEAT-02): SQLite 저장 계층 구현 (storage.py)

- src/storage.py 신규: 설계서 3장 DDL(articles + meta + 인덱스)
- init_db: 디렉토리 자동 생성 + 스키마 초기화
- insert_article: INSERT OR IGNORE, 신규/중복 여부 bool 반환
- update_analysis: tags list → JSON 직렬화 후 UPDATE
- get_articles_by_range: collected_at BETWEEN, tags JSON 역직렬화
- get_meta / set_meta: key-value upsert (ON CONFLICT DO UPDATE)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-03 — 수집기 (RSS + raw JSON) + 소스 접근성 진단 + 샘플 fixtures
- 처리 이슈: #3
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/collector.py` | RSS 수집기 신규 (collect, save_raw_json, check_sources, format_diagnostics) |
| `tests/fixtures/sample_articles.json` | 6건 샘플 (중복 URL https://ex.com/a 2건, published_at 다양) |

### 구현 내용
- `_to_iso(v)`: dateutil.parser 기반 타임스탬프 → ISO8601 변환, 실패 시 None
- `_in_range(pub_iso, start, end)`: published_at 범위 필터 (None이면 보호적 채택)
- `save_raw_json(raw_dir, source, day, payload)`: `data/raw/<date>/<source>.json` 저장
- `collect(sources, range_start, range_end, raw_dir, timeout, test_fixtures, save_raw)`:
  - `test_fixtures` 지정 시: JSON 로드 → 정규화 → 범위 필터 → 반환 (네트워크 없음)
  - 일반 경로: enabled 소스만 requests.get → feedparser.parse → 범위 필터
  - `save_raw=False`면 `save_raw_json` 호출 자체를 건너뜀 (파일 0개 보장)
  - 단일 소스 예외 → `errors.append({source, error})` 후 계속 (전체 중단 없음)
- `check_sources(sources, timeout)`: 읽기 전용 진단, 설계서 9-1 항목 dict 리스트 반환
  - DB/파일/Discord 쓰기 없음
  - Timeout 예외 → timeout=True, 일반 예외 → error 필드
- `format_diagnostics(rows)`: 콘솔 표 문자열 (헤더 + 행 + 요약 줄)

### 수동 검증 방법 (Bash 차단 환경용 — FEAT-03 섹션 9 기반)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
python -c "
import os, glob, shutil
from src.collector import collect, check_sources, format_diagnostics

# 1. --test: 6건 전부 반환 (범위 무제한)
arts, errs = collect([], '2026-01-01T00:00:00', '2026-12-31T23:59:59', 'data/raw_t',
                     test_fixtures='tests/fixtures/sample_articles.json')
print('[1] 6건:', len(arts), '/ 오류:', errs)   # 6, []

# 2. save_raw=False → raw 파일 0개 (dry-run 무상태)
shutil.rmtree('data/raw_t2', ignore_errors=True)
collect([], '2026-01-01T00:00:00', '2026-12-31T23:59:59', 'data/raw_t2',
        test_fixtures='tests/fixtures/sample_articles.json', save_raw=False)
print('[2] raw 파일:', glob.glob('data/raw_t2/**/*.json', recursive=True))  # []

# 3. 범위 필터: 06-15~06-30 → a(×2) + b = 3건
arts2, _ = collect([], '2026-06-15T00:00:00', '2026-06-30T23:59:59', 'data/raw_t',
                   test_fixtures='tests/fixtures/sample_articles.json', save_raw=False)
print('[3] 범위 필터 3건:', len(arts2))   # 3

# 4. check_sources — 연결 불가 소스 → ok=False, error 있음
rows = check_sources([{'name': 'Bad', 'url': 'http://127.0.0.1:9/none', 'enabled': True}], timeout=2)
print('[4]', rows[0]['ok'], bool(rows[0]['error']))   # False True
print(format_diagnostics(rows))

# 정리
shutil.rmtree('data/raw_t', ignore_errors=True)
shutil.rmtree('data/raw_t2', ignore_errors=True)
print('=== 검증 완료 ===')
"
```

### 구현 판단 / 특이사항
- Bash 차단으로 로컬 자동 실행 불가 — 수동 검증 스니펫 제공 (FEAT-03 섹션 9와 동일)
- `_in_range` 에서 범위 비교는 ISO8601 문자열 사전순으로 충분 (날짜 포맷 일관성 보장)
- `collect`의 test_fixtures 경로: `with open(test_fixtures)` 사용 (FEAT 문서의 `json.load(open(...))` 개선)
- check_sources는 `enabled` 무관 전체 소스 처리 (FEAT-03 섹션 4: "enabled 무관, 끔은 표시만")
- 부분 실패: 소스 예외 → errors.append 후 loop 계속, 전체 중단 없음

### 범위 준수 확인
- DB 저장 collector 내부 없음 (pipeline → FEAT-02 호출)
- check_sources 읽기 전용, DB/파일/Discord 쓰기 없음
- save_raw=False 시 save_raw_json 미호출 (raw 파일 0개 보장)
- 분석/요약 로직 선구현 없음
- GitHub/Reddit/스크래핑 추가 없음 (V0.2)

### 제안 commit 메시지
```
feat(FEAT-03): RSS 수집기 + 소스 진단 + 샘플 fixtures 구현

- src/collector.py 신규: collect(save_raw 옵션, test_fixtures --test 경로),
  save_raw_json, check_sources(읽기 전용, 9-1 항목), format_diagnostics(표 출력)
- tests/fixtures/sample_articles.json 신규: 6건, 중복 URL 1쌍(QA-2 검증용)
- save_raw=False면 raw 파일 0개 (dry-run 무상태 보장)
- 단일 소스 실패 → errors 기록 후 계속 (부분 실패 처리)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-04 — Claude 분석기 + fallback + 프로필 프롬프트
- 처리 이슈: #4
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/analyzer.py` | Claude 분석기 신규 (analyze, _build_prompt, _call_claude, _parse_response, _fallback_analyze) |

### 구현 내용
- `FALLBACK_KEYWORDS`: claude·anthropic·openai·gpt·codex·mcp·agent·gemini·cursor·automation·github actions·n8n
- `analyze(articles, operator_profile, api_key, model, max_output_tokens, max_articles, force_fallback)`:
  - `force_fallback=True` 또는 `api_key=None` → 즉시 `_fallback_analyze` 반환 (mode="fallback")
  - 아니면 `_build_prompt` → `_call_claude` → `_parse_response` → mode="claude" 반환
  - 모든 예외(API/네트워크/타임아웃/모델명 오류/JSON 파싱 실패) → except에서 fallback 강등 + ERROR 로그
- `_build_prompt(articles, operator_profile)`: system/user 메시지 dict 반환. system에 `<운영자 프로필>` 렌즈 주입. user에 기사 목록 JSON + 출력 스키마.
- `_call_claude(prompt, api_key, model, max_tokens)`: 런타임 `from anthropic import Anthropic` (SDK 미설치 시 예외 → fallback). 모델명 오류 시 SDK가 NotFoundError/BadRequest → except가 fallback으로 흡수.
- `_parse_response(text, articles)`: 첫 `{`~마지막 `}` 슬라이스 후 json.loads. 누락 필드 기본값 보정(headline_changes/sangsang_ideas/action_items=[], summary_text="", importance/relevance=0). `{` 없으면 ValueError 발생 → analyze가 fallback으로 흡수.
- `_fallback_analyze(articles)`: 키워드 매칭 tags + excerpt 앞 2문장 summary + hot 제목 briefing. 항상 mode="fallback".

### 수동 검증 방법 (Bash 차단 환경용)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
python -c "
from src.analyzer import analyze, _parse_response

arts = [
    {'url':'u1','title':'Claude Code 업데이트','source':'s','published_at':None,'raw_excerpt':'MCP 지원 강화. 두번째 문장.'},
    {'url':'u2','title':'일반 경제 뉴스','source':'s2','published_at':None,'raw_excerpt':'경기 회복세 지속. 전문가 분석.'},
]
profile = '프로필'

# 검증 1: api_key=None → mode='fallback', tags에 키워드 매칭
r = analyze(arts, profile, api_key=None)
print('[1] mode:', r['mode'])                        # fallback
print('[1] u1 tags:', r['articles'][0]['tags'])      # ['claude', 'mcp']
assert r['mode'] == 'fallback'
assert 'claude' in r['articles'][0]['tags']
assert 'mcp' in r['articles'][0]['tags']

# 검증 2: force_fallback=True → 무조건 fallback
r2 = analyze(arts, profile, api_key='sk-fake-key', force_fallback=True)
print('[2] mode:', r2['mode'])                       # fallback
assert r2['mode'] == 'fallback'

# 검증 3: 잘못된 모델명 + 가짜 키 → 예외가 fallback으로 흡수 (네트워크 후 except)
r3 = analyze(arts, profile, api_key='sk-fake-key', model='no-such-model-xyz')
print('[3] mode:', r3['mode'])                       # fallback (중단 없음)
assert r3['mode'] == 'fallback'

# 검증 4: _parse_response → 누락 필드 보정
fake = '{\"articles\":[{\"url\":\"u1\",\"summary\":\"요약\",\"tags\":[\"x\"],\"importance\":4,\"relevance\":3}],\"briefing\":{\"summary_text\":\"hi\"}}'
parsed = _parse_response(fake, arts)
print('[4] headline_changes:', parsed['briefing']['headline_changes'])  # []
print('[4] importance:', parsed['articles'][0]['importance'])           # 4
assert parsed['briefing']['headline_changes'] == []
assert parsed['articles'][0]['importance'] == 4

# 검증 5: 모든 반환 dict에 mode/articles/briefing 키 보유
for result in [r, r2, r3]:
    assert 'mode' in result and 'articles' in result and 'briefing' in result

print()
print('=== FEAT-04 완료 조건 전부 통과 ===')
"
```

### 구현 판단 / 특이사항
- Bash 차단으로 로컬 자동 실행 불가 — 수동 검증 스니펫 제공 (FEAT-04 섹션 9 기반)
- `_call_claude`의 `from anthropic import Anthropic`을 런타임 임포트로 분리: SDK 미설치 환경에서도 모듈 임포트 성공, 실제 API 호출 시점에 예외 → fallback 흡수
- `_parse_response`에서 `{` 없는 경우 명시적 ValueError 발생 → analyze except가 잡아 fallback 강등 (무한 루프 없음)
- `force_fallback` 및 `api_key=None` 분기는 네트워크 시도 없이 즉시 fallback (비용 0)
- 모델명 오류 시 Anthropic SDK가 NotFoundError/InvalidRequestError 계열 예외를 던지며, analyze의 `except Exception`이 전부 포착 → mode="fallback" 반환, 파이프라인 중단 없음

### 범위 준수 확인
- DB/파일/Discord 쓰기 없음 (dict 반환만)
- 실제 API 키 코드/문서 미기재
- Claude Code headless 등 다른 연동 방식 선구현 없음 (V0.2)
- 리포트/Discord 텍스트 조립 없음 (FEAT-05/06)
- DB 갱신 없음 (pipeline이 FEAT-02 호출)

### 제안 commit 메시지
```
feat(FEAT-04): Claude 분석기 + fallback + 운영자 프로필 렌즈 주입 구현

- src/analyzer.py 신규: analyze, _build_prompt, _call_claude, _parse_response, _fallback_analyze
- operator_profile을 <운영자 프로필> 렌즈로 Claude 시스템 프롬프트에 주입
- 모든 예외(모델명 오류·API 실패·JSON 파싱 실패) → fallback 흡수, 파이프라인 중단 없음
- force_fallback=True / api_key=None → 즉시 fallback 반환 (네트워크 미시도)
- 반환 dict 항상 mode('claude'|'fallback') / articles / briefing 키 보유

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-05 — Markdown 리포트 생성기
- 처리 이슈: #5
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/reporter.py` | 리포트 생성기 신규 (build_report, save_report, _section, _by_tags) |

### 구현 내용
- `_section(title, items)`: items 없으면 `_해당 없음_`, 있으면 `- item` 목록 반환.
- `_by_tags(articles, keys)`: tags에 keys 교집합이 있는 기사를 `[title](url) — summary` 형태로 반환. `a.get('tags',[])` 보호.
- `build_report(date, range_start, range_end, articles, briefing, mode, catchup, failed_sources, new_count, profile_is_default)`:
  - 헤더 블록: 제목·날짜, 모드 배지(fallback 시 `> **AI 분석 실패 / 기본 리포트 생성**`), catch-up·프로필부재·실패소스 경고, 수집 범위·건수·모드 메타.
  - 7개 섹션: 오늘의 핵심 변화 / Claude 관련 / OpenAI 관련 / MCP·Agent 관련 / 상상공방 아이디어 / 액션 아이템 / 원문 링크(importance 내림차순).
  - articles/briefing 빈 값 → 각 섹션 `_해당 없음_`으로 안전 출력.
- `save_report(reports_dir, date, content)`:
  - `os.makedirs(reports_dir/<date>, exist_ok=True)` 후 `report.md` 기록.
  - 같은 날 재실행 시 덮어씀. 저장 경로 반환.

### 수동 검증 방법 (Bash 차단 환경용 — FEAT-05 섹션 9 기반)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
# QA-5 / QA-6 / QA-3 / 7개 섹션 — 임시 경로(reports/test_feat05) 사용
python -c "
import shutil
from src.reporter import build_report, save_report

arts = [{'url':'u1','title':'Claude Code','summary':'요약','tags':['claude'],'importance':5}]
b = {'headline_changes':['x'],'sangsang_ideas':[],'action_items':[],'summary_text':'s'}

md = build_report(
    '2026-06-21',
    '2026-06-01T00:00:00', '2026-06-21T00:00:00',
    arts, b,
    'fallback', catchup=True, new_count=1
)

# QA-5: fallback 문구 정확히 포함
ok5 = 'AI 분석 실패 / 기본 리포트 생성' in md
print('QA-5 fallback 문구:', ok5)   # True

# QA-6: catch-up 수집 범위 날짜 포함
ok6 = '2026-06-01' in md
print('QA-6 catchup 범위:', ok6)    # True

# QA-3: save_report → reports/test_feat05/2026-06-21/report.md 생성
path = save_report('reports/test_feat05', '2026-06-21', md)
import os; ok3 = os.path.isfile(path)
print('QA-3 파일 생성:', ok3, path)  # True

# 7개 섹션 헤더 존재 확인
sections = [
    '## 오늘의 핵심 변화',
    '## Claude / Claude Code 관련 소식',
    '## OpenAI / Codex / GPT 관련 소식',
    '## MCP / Agent / 자동화 관련 소식',
    '## 상상공방에 적용할 수 있는 아이디어',
    '## 나중에 실험해볼 액션 아이템',
    '## 저장해둘 원문 링크',
]
ok_sec = all(s in md for s in sections)
print('7개 섹션 존재:', ok_sec)  # True

# 정리
shutil.rmtree('reports/test_feat05', ignore_errors=True)
print('=== FEAT-05 완료 조건 전부 통과 ===' if all([ok5,ok6,ok3,ok_sec]) else '=== 실패 ===')
"
```

### 구현 판단 / 특이사항
- Bash 차단으로 로컬 자동 실행 불가 — 수동 검증 스니펫 제공
- FEAT 문서 섹션 6의 코드 예시를 그대로 반영하되 docstring·타입 힌트·공백 정리로 가독성 개선 (동작 동일)
- `_by_tags`의 tags 대소문자 처리: `k.lower()` 로 정규화 (FEAT 문서 동일)
- `save_report` 반환값은 저장된 경로(str) — QA-3 자동 확인 가능

### 범위 준수 확인
- 수집/분석/전송 로직 없음 (FEAT-03/04/06 선구현 없음)
- Discord 2000자 메시지 생성 없음 (FEAT-06)
- HTML/PDF 변환 없음
- 리팩터링 범위 이탈 없음

### 제안 commit 메시지
```
feat(FEAT-05): Markdown 일일 리포트 생성기 구현 (reporter.py)

- src/reporter.py 신규: build_report(7개 섹션 Markdown), save_report(날짜별 폴더)
- mode='fallback' 시 상단에 정확히 'AI 분석 실패 / 기본 리포트 생성' 문구 삽입(QA-5)
- catchup=True 시 수집 범위 날짜 본문 등장(QA-6)
- save_report → reports/<date>/report.md 생성, 재실행 시 덮어씀(QA-3)
- articles/briefing 빈 값 → 각 섹션 '_해당 없음_' 안전 출력

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-07 — 파이프라인 오케스트레이션
- 처리 이슈: #7
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/pipeline.py` | 파이프라인 오케스트레이션 신규 (run, _determine_range, _dispatch_check_sources) |

### 구현 내용
- `_iso(dt)`: datetime → ISO8601 문자열 변환 헬퍼.
- `_determine_range(args, conn, cfg)`:
  - 우선순위: `--from/--to` 명시 > `last_success_at`(meta) > `lookback_hours` 기본값.
  - `conn=None`(dry-run) 시 last_success_at 미참조.
  - `--test` 모드에서 명시 범위 없으면 lookback을 720h(30일)로 확장 — fixtures 날짜(~30일 이내 분포) 안정적 커버.
  - catchup: `--from/--to` 있거나 last_success_at이 lookback보다 오래된 경우 True.
- `_dispatch_check_sources(sources, timeout)`:
  - `check_sources` → `format_diagnostics` → print. 파이프라인 미실행.
  - 실패 소스 있으면 종료코드 1(진단 신호), 예외 없음.
- `run(args) -> int`:
  - 설정 로드: `load_config()` → `load_secrets()` → `load_sources()` → `load_operator_profile()`.
  - `paths.sources`·`paths.operator_profile` config 미포함 시 기본값 사용.
  - `--check-sources` → `_dispatch_check_sources` 후 즉시 반환.
  - **dry-run 무상태**: `init_db` 건너뜀 (DB 파일 생성 자체 방지). `collect(save_raw=False)`. 수집 직후 preview 출력 후 `return 0`. 이 시점까지 DB·raw·report·Discord·meta 어떤 쓰기도 없음.
  - 일반 경로: `init_db` → `_determine_range` → `collect` → `insert_article`(중복 무시) → `analyze`(실패 시 fallback) → `update_analysis` → `build_report` → `save_report` → Discord 전송(실패 시 계속) → `set_meta`.
  - DB 쓰기 오류는 예외 전파 → 종료코드 1 + last_success_at 미갱신(다음 실행 catch-up 유도).
  - Discord/Claude 실패는 파이프라인 지속.

### 구현 판단 / 특이사항
- **config.yaml 키 매핑**: FEAT-07 설계 템플릿이 `cfg["collection"]["default_lookback_hours"]`·`cfg["analysis"]["model"]` 등을 참조하나 실제 config.yaml은 `pipeline.lookback_hours`·`claude.model`·`claude.max_tokens`·`pipeline.max_articles_per_source`로 구성됨. 실제 키로 매핑, 없으면 안전한 기본값 사용.
- **--test 30일 lookback**: 기본 24h lookback으로는 fixtures(published_at ~30일 내)가 시간대에 따라 범위 밖이 될 수 있음. --test에서 lookback을 720h(30일)로 확장해 신뢰성 확보. 이 판단을 빌드노트에 기록(범위 이탈 아님 — fixtures 커버를 위한 보조 정책).
- **dry-run에서 init_db 건너뜀**: FEAT-07 섹션 5 주석 "더 엄격히 하려면 dry-run일 때 init_db도 건너뛸 수 있다"에 따라 선택. `test ! -e data/dr.db` 검증을 통과하려면 필수.
- **`_determine_range(conn=None)`**: dry-run 시 conn을 None으로 전달해 last_success_at 미참조. get_meta 호출 전 `if conn is not None` 가드.
- Bash 차단으로 로컬 자동 실행 불가 — 수동 검증 스니펫 제공.

### 수동 검증 방법 (Bash 차단 환경용)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행.
**운영 경로 비오염을 위해 env override 경로 사용.**

```bash
# 0. 의존성 설치 (최초 1회)
pip install -r requirements.txt

# 1. import 확인
python -c "from src.pipeline import run, _determine_range, _dispatch_check_sources; print('import OK')"

# 2. QA-1: --test --no-discord → 종료코드 0 + DB 기사 저장 + report.md 생성
#    (운영 경로 비오염: 테스트 전용 경로 사용)
rm -rf /tmp/amb_test_db /tmp/amb_test_reports /tmp/amb_test_raw
MORNINGBRIEF_DB=/tmp/amb_test_db/test.db \
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_test_reports \
MORNINGBRIEF_RAW_DIR=/tmp/amb_test_raw \
  python main.py --test --no-discord
echo "exit=$?"

sqlite3 /tmp/amb_test_db/test.db "SELECT COUNT(*) FROM articles;"
# 기대: 5 (6건 수집, 중복 URL 1건 무시)
ls /tmp/amb_test_reports/$(date +%F)/report.md && echo "리포트 생성 OK"

# 3. QA-2: --test 2회 실행해도 중복 URL 1행만
MORNINGBRIEF_DB=/tmp/amb_test_db/test.db \
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_test_reports \
MORNINGBRIEF_RAW_DIR=/tmp/amb_test_raw \
  python main.py --test --no-discord
sqlite3 /tmp/amb_test_db/test.db "SELECT url,COUNT(*) c FROM articles GROUP BY url HAVING c>1;"
# 기대: 0행 (중복 없음)

# 4. QA-5: --force-fallback → 리포트에 fallback 문구
MORNINGBRIEF_DB=/tmp/amb_test_db/test.db \
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_test_reports \
MORNINGBRIEF_RAW_DIR=/tmp/amb_test_raw \
  python main.py --test --no-discord --force-fallback
grep "AI 분석 실패 / 기본 리포트 생성" /tmp/amb_test_reports/$(date +%F)/report.md && echo "QA-5 OK"

# 5. QA-6: --from/--to → 리포트에 범위 명시 + catchup 표시
MORNINGBRIEF_DB=/tmp/amb_test_db/test.db \
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_test_reports \
MORNINGBRIEF_RAW_DIR=/tmp/amb_test_raw \
  python main.py --test --no-discord --from "2026-06-01T00:00:00" --to "2026-06-21T23:59:59"
grep "2026-06-01" /tmp/amb_test_reports/$(date +%F)/report.md && echo "QA-6 날짜 OK"
grep "catch-up\|보완 수집" /tmp/amb_test_reports/$(date +%F)/report.md && echo "QA-6 catchup OK"

# 6. QA dry-run 무상태: DB 파일·reports·raw 디렉토리 미생성 확인
rm -rf /tmp/amb_dr_db /tmp/amb_dr_reports /tmp/amb_dr_raw
MORNINGBRIEF_DB=/tmp/amb_dr_db/dr.db \
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_dr_reports \
MORNINGBRIEF_RAW_DIR=/tmp/amb_dr_raw \
  python main.py --test --dry-run
test ! -e /tmp/amb_dr_db/dr.db && \
test ! -d /tmp/amb_dr_reports && \
test ! -d /tmp/amb_dr_raw && echo "dry-run 무상태 OK"

# 7. --check-sources: 진단표만 출력 (파이프라인 미실행)
python main.py --check-sources
echo "exit=$?"  # 소스 상태에 따라 0 또는 1

# 8. 정리
rm -rf /tmp/amb_test_db /tmp/amb_test_reports /tmp/amb_test_raw
rm -rf /tmp/amb_dr_db /tmp/amb_dr_reports /tmp/amb_dr_raw
```

### 범위 준수 확인
- 모듈 내부 로직 재구현 없음 (FEAT-02~06 함수 호출만).
- dry-run에서 DB·raw·report·Discord·meta 어떤 쓰기도 없음.
- Discord/Claude 실패를 치명 오류로 처리하지 않음 (파이프라인 지속).
- `--check-sources` 후 파이프라인 미실행.
- launchd/테스트/문서(FEAT-08) 선구현 없음.
- git/gh 실행 없음.

### 제안 commit 메시지
```
feat(FEAT-07): 파이프라인 오케스트레이션 구현 (pipeline.py)

- src/pipeline.py 신규: run(), _determine_range(), _dispatch_check_sources()
- end-to-end 흐름: 설정→범위결정→수집→DB저장→분석→리포트→Discord→meta갱신
- dry-run 무상태: init_db 건너뜀 + save_raw=False, DB·raw·report·Discord·meta 쓰기 없음
- --test 30일 lookback: fixtures 날짜를 시간대 무관하게 안정적 커버
- 실패 복구: 단일소스/Claude/Discord 실패 시 파이프라인 지속, DB 오류만 종료코드 1
- --check-sources: check_sources+format_diagnostics 출력 후 파이프라인 미실행

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-08 — 스케줄러(launchd) + 통합테스트 + 문서
- 처리 이슈: #8
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 추가 문서 참조 이유
설계서 9-3·9-5·9-6 을 콕 집어 읽음 — README의 "V0.1 성공 기준 4개 명령" 출처(9-3)와 plist 템플릿(9-5), README 필수 섹션 순서(9-6) 확인이 필요했음.

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `tests/test_pipeline.py` | pytest 통합 테스트 (QA-1~6, 경로 env override로 운영 데이터 격리) |
| `scripts/com.itsangsang.morningbrief.plist` | launchd 04:30 실행 설정 (절대경로 자리표시자 포함) |
| `README.md` | 설치→.env→sources.yaml→--check-sources→모델명설정→--test→launchd 등록 + V0.1 성공 기준 4개 명령 |

### 수동 검증 방법 (Bash 차단 환경용)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
# 0. 의존성 설치 (최초 1회)
pip install -r requirements.txt
pip install pytest
cp config/sources.example.yaml config/sources.yaml

# 1. pytest 통합 테스트 — QA-1~6 전부 통과 확인
pytest -q tests/test_pipeline.py
# 기대: 6 passed

# 2. 운영 DB 무변경 확인
BEFORE=$(test -e data/morning_brief.db && stat -f %m data/morning_brief.db || echo none)
pytest -q tests/test_pipeline.py
AFTER=$(test -e data/morning_brief.db && stat -f %m data/morning_brief.db || echo none)
[ "$BEFORE" = "$AFTER" ] && echo "운영 DB 무변경 OK"

# 3. 테스트 산출물은 별도 경로에 생성됨 확인
ls data/test_morning_brief.db reports/test/   2>/dev/null
# (테스트 후 teardown fixture가 정리하므로 없을 수 있음)

# 4. plist XML 유효성 확인 (macOS)
plutil -lint scripts/com.itsangsang.morningbrief.plist  # OK

# 5. README 필수 항목 포함 확인
grep -q "check-sources" README.md && echo "check-sources OK"
grep -q "V0.1 성공 기준" README.md && echo "V0.1 기준 OK"
grep -q "claude.model" README.md && echo "모델명 안내 OK"
```

### 구현 판단 / 특이사항
- FEAT 문서 코드에서 `ROOT = os.path.dirname(os.path.dirname(__file__))` 를 `os.path.abspath(__file__)` 기반으로 보강해 pytest 실행 위치와 무관하게 ROOT가 항상 절대경로가 되도록 함.
- clean fixture에 teardown(yield 이후) 블록을 추가해 테스트 실패 시에도 테스트 디렉토리를 정리하도록 구현. 운영 경로에는 절대 접근하지 않음.
- 설계서 9-6에서 "config.yaml의 `analysis.model`" 이라고 표기됐으나 실제 config.yaml 키는 `claude.model` 임. README는 실제 키(`claude.model`)를 기준으로 작성.
- Bash 차단으로 pytest 로컬 자동 실행 불가 — 수동 검증 스니펫 제공. commit 가능 상태로 정리.

### 범위 준수 확인
- FEAT-01~07 파이프라인·모듈 로직 수정 없음.
- 테스트는 TEST_DB/TEST_RAW/TEST_REPORTS 경로만 사용, 운영 morning_brief.db·reports/ 미접촉.
- cron 설정 없음 (launchd 채택).
- GitHub Actions/서버 배포 문서화 없음 (V0.2).

### 제안 commit 메시지
```
feat(FEAT-08): 통합테스트(pytest QA-1~6) + launchd plist + README 완성

- tests/test_pipeline.py 신규: pytest QA-1~6, 경로 env override로 운영 DB·reports/ 완전 격리
- scripts/com.itsangsang.morningbrief.plist 신규: launchd 04:30 실행 설정 (절대경로 자리표시자)
- README.md 완성: 설치→환경설정→소스진단→모델명→테스트→V0.1 성공기준 4개 명령→launchd 등록
- V0.1 전체 완성 (FEAT-01~08 모두 구현)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## #10 — V0.1.1 패치: config.yaml request_timeout_sec 키 노출
- 처리 이슈: #10
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `config/config.yaml` | `pipeline` 섹션에 `request_timeout_sec: 15` + 한 줄 주석 추가 |
| `README.md` | CLI 옵션 > 환경변수 override 표 아래 `config.yaml 파이프라인 설정` 표 1행 추가 |

### 구현 판단
- `pipeline.py`의 기본 15초 동작은 그대로 유지. config에 키만 명시적으로 노출 (동작 변경 없음).
- 코드 로직(pipeline.py 등) 수정 없음.

### 제안 commit 메시지
```
fix(#10): config.yaml에 request_timeout_sec 키 명시 노출 (V0.1.1)

- config/config.yaml pipeline 섹션에 request_timeout_sec: 15 추가 (주석 포함)
- README.md CLI 옵션 섹션에 pipeline.request_timeout_sec 설정 표 한 줄 보강
- 코드 로직·기본값 변경 없음, 키 노출만

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## #9 — V0.1.1 패치: pytest teardown 빈 디렉토리 잔류 해결
- 처리 이슈: #9
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `tests/test_pipeline.py` | (1) `clean` fixture를 `tmp_path` + `monkeypatch` 방식으로 전환, (2) `_run()` 기본 커맨드에 `--force-fallback` 추가 |

### 수정 내용

**[1] 빈 디렉토리 잔류 — `pytest tmp_path` 전환**

- **구 방식**: `TEST_RAW = "data/raw/test"` 리프를 `shutil.rmtree`로 제거 → 부모 `data/raw/` 가 빈 채 잔류
- **신 방식**: `tmp_path` (OS 임시 디렉토리) + `monkeypatch.setenv`로 `MORNINGBRIEF_*` 3개 경로를 테스트별 임시 경로로 주입 → pytest가 자동 정리, `data/` 아래 어떤 경로도 오염 없음

구체적 변경:
1. `import shutil` 제거 (더 이상 불필요)
2. 모듈 수준 상수 `TEST_DB / TEST_RAW / TEST_REPORTS` 제거
3. `_env()`: `os.environ.copy()` 단순화 (monkeypatch가 이미 `MORNINGBRIEF_*` 설정)
4. `clean(tmp_path, monkeypatch)`: 3개 env를 `tmp_path` 기반 절대경로로 주입, yield 후 별도 teardown 없음
5. `_test_db_path()`: `os.environ["MORNINGBRIEF_DB"]` 반환
6. `_test_report()`: `os.environ["MORNINGBRIEF_REPORTS_DIR"]` 기반 경로 반환
7. QA-1~6 테스트 함수 본체 무변경 (검증 의미 동일)

**[2] 실제 Claude API 호출 차단 — hermetic `--force-fallback` 전환**

- **원인**: `.env`에 실제 `ANTHROPIC_API_KEY` 가 있어 pytest가 실제 Claude API를 호출, 214초 소요·과금 발생
- **수정**: `_run()` 기본 커맨드에 `--force-fallback` 추가
  - 구: `["main.py", "--test", "--no-discord", *flags]`
  - 신: `["main.py", "--test", "--no-discord", "--force-fallback", *flags]`
- fallback 모드에서도 QA-1~6 검증 의미 전부 유지:
  - QA-1 DB 저장 / QA-2 중복제거 / QA-3 리포트 생성 / QA-4 종료코드 0 / QA-6 범위 날짜 — fallback 경로에서 모두 성립
  - QA-5: 이미 `_run("--force-fallback")` 명시 → 중복이지만 `store_true` 라 무해
  - "claude 모드"를 검증하는 QA 없음 — 해당 없음
- 결과: 실제 Claude API 미호출, 수 초 내 6 passed

### 수동 검증 방법
```bash
# projects/AI-Morning-Brief/05-개발/ 기준
cp config/sources.example.yaml config/sources.yaml  # 최초 1회
pytest -q tests/test_pipeline.py
# 기대: 6 passed (수 초 내 — Claude API 호출 없음)

# 테스트 후 data/ 아래 잔류 여부 확인
find data/ -type d -empty 2>/dev/null
# 기대: 출력 없음 (빈 디렉토리 잔류 없음)
```

### 범위 준수 확인
- `tests/test_pipeline.py` 만 변경 — src/ 파이프라인·모듈 코드 무변경
- 운영 `data/morning_brief.db` · `reports/` (test 제외) 미접촉
- QA-1~6 검증 의미(샘플 저장/중복제거/리포트 생성/Discord 비활성/fallback/catch-up 범위) 그대로 유지

### 제안 commit 메시지
```
fix(#9): pytest hermetic 격리 — tmp_path 전환 + force-fallback 기본화 (V0.1.1)

- tests/test_pipeline.py: clean fixture를 tmp_path + monkeypatch 방식으로 전환
  (data/raw/ 빈 디렉토리 잔류 해소, OS 임시 디렉토리 완전 격리)
- _run() 기본 커맨드에 --force-fallback 추가 — 실제 Claude API 미호출
  (.env ANTHROPIC_API_KEY 존재 시에도 과금·네트워크 의존 없이 수 초 내 6 passed)
- QA-1~6 검증 로직 무변경

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## #11 — V0.1.1 패치: 리포트 저장 경로 구조 변경
- 처리 이슈: #11
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 경로 구조 before/after
| | 구조 | 예시 |
|---|---|---|
| before | `reports/<YYYY-MM-DD>/report.md` | `reports/2026-06-21/report.md` |
| after | `reports/<YYYY>/<MM>/<DD>.md` | `reports/2026/06/21.md` |

장기 운영 시 reports 루트에 날짜 폴더(YYYY-MM-DD)가 날마다 누적되는 문제 해소.
연/월 디렉토리 아래 DD.md 파일로 계층화해 탐색 편의성 확보.

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `src/reporter.py` | `save_report`: 경로 생성 로직을 `YYYY/MM/DD.md` 구조로 변경, docstring 갱신 |
| `src/notifier.py` | `build_briefing_message` docstring의 `report_path` 파라미터 예시를 새 경로 형식으로 갱신 (기능 코드 무변경) |
| `tests/test_pipeline.py` | `_test_report()`: 경로 계산 로직을 `YYYY/MM/DD.md` 구조로 변경, `_test_report` 및 QA-3 docstring 갱신 |
| `README.md` | 파일 구조 섹션에서 `reports/YYYY-MM-DD/report.md` → `reports/YYYY/MM/DD.md` |

### save_report 시그니처 변화 여부
변화 없음. `save_report(reports_dir, date, content) -> str` 그대로 유지.
`pipeline.py` 호출부 수정 불필요.

### dry-run 무상태 확인
`save_report`는 dry-run 분기(`if args.dry_run: return 0`) 이후에만 호출된다.
dry-run 실행 경로에서 `save_report` 자체가 호출되지 않으므로 새 경로에도 파일이 생성되지 않음 — 무상태 절대 유지.

### 수동 검증 방법

터미널에서 `projects/AI-Morning-Brief/05-개발/` 기준 실행:

```bash
# 1. 정상 파이프라인 — 새 경로에 리포트 생성 확인
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_11_test \
  python main.py --test --no-discord
echo "exit=$?"
ls /tmp/amb_11_test/$(date +%Y)/$(date +%m)/$(date +%d).md && echo "새 경로 리포트 OK"
rm -rf /tmp/amb_11_test

# 2. force-fallback 경로
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_11_fb \
  python main.py --test --no-discord --force-fallback
echo "exit=$?"
ls /tmp/amb_11_fb/$(date +%Y)/$(date +%m)/$(date +%d).md && echo "fallback 새 경로 OK"
rm -rf /tmp/amb_11_fb

# 3. dry-run 무상태 — 새 경로에도 파일 생성 없음 확인
MORNINGBRIEF_REPORTS_DIR=/tmp/amb_11_dr \
  python main.py --test --dry-run
echo "exit=$?"
test ! -d /tmp/amb_11_dr && echo "dry-run 무상태 OK"

# 4. pytest — 6 passed
pytest -q tests/test_pipeline.py
```

### 범위 준수 확인
- `pipeline.py` 로직 재구현 없음 (호출부 수정 불필요)
- dry-run 무상태 절대 유지 (경로 변경이 dry-run 분기에 영향 없음)
- 운영 데이터(data/morning_brief.db) 손상 없음
- QA-1~6 검증 의미 전부 유지

### 제안 commit 메시지
```
fix(#11): 리포트 저장 경로 구조 변경 — YYYY-MM-DD/report.md → YYYY/MM/DD.md (V0.1.1)

- src/reporter.py: save_report 경로를 reports/<YYYY>/<MM>/<DD>.md 로 변경
  (연/월 자동 디렉토리 생성, 같은 날 재실행 시 덮어씀, 시그니처 유지)
- src/notifier.py: report_path docstring 예시를 새 경로 형식으로 갱신 (기능 무변경)
- tests/test_pipeline.py: _test_report() 경로 계산을 YYYY/MM/DD.md 구조로 수정
  (QA-1~6 검증 의미·hermetic 격리 그대로 유지)
- README.md: 파일 구조 섹션 reports 경로 설명 갱신

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-06 — Discord Webhook 전송기
- 처리 이슈: #6
- 구현일: 2026-06-21
- 담당: builder (claude-sonnet-4-6)

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/notifier.py` | Discord 전송기 신규 (build_briefing_message, send_discord) |

### 구현 내용
- `build_briefing_message(date, range_start, range_end, new_count, briefing, mode, catchup, failed_sources, report_path, profile_is_default, char_limit=2000, truncate_marker)`:
  - 날짜 헤더 / mode='fallback' 안내 / catch-up 안내 / 프로필 부재 안내 / 수집 범위+건수 / 핵심 변화(최대 5개) / 상상공방 적용 / 실패 소스 / 리포트 경로 를 조립
  - `len(msg) > char_limit`이면 `msg[:char_limit-len(marker)] + marker`로 절삭
  - 항상 길이 ≤ char_limit 문자열 반환
- `send_discord(webhook_url, message)`:
  - webhook_url이 None/빈값 → `logging.warning` + `return False` (예외 없음)
  - `requests.post(url, json={"content": message}, timeout=15)`: 2xx → True
  - HTTP 비2xx → ERROR 로그 + False
  - 모든 예외 → ERROR 로그 + False (파이프라인 중단 없음)

### 수동 검증 방법 (Bash 차단 환경용 — FEAT-06 섹션 9 기반)

터미널에서 `projects/AI-Morning-Brief/05-개발/` 폴더 기준으로 실행:

```bash
python -c "
from src.notifier import build_briefing_message, send_discord
b={'headline_changes':['a','b','c'],'sangsang_ideas':['아이디어'],'action_items':[],'summary_text':''}
m=build_briefing_message('2026-06-21','s','e',3,b,mode='fallback',report_path='reports/2026-06-21/report.md')
print(len(m)<=2000, 'AI 분석 실패' in m)        # True True
print(send_discord(None, m))                     # False (예외 없음)
"

# 긴 입력 절삭 검증
python -c "
from src.notifier import build_briefing_message
import string
long_hc = ['변화' + str(i) + 'x'*200 for i in range(5)]
b = {'headline_changes': long_hc, 'sangsang_ideas': ['아이' * 100]}
m = build_briefing_message('2026-06-21', 's', 'e', 999, b)
marker = '…(이하 생략, 전체는 로컬 리포트 참조)'
print('길이:', len(m), '<= 2000:', len(m) <= 2000)   # True
print('마커 끝:', m.endswith(marker))                  # True
"

# failed_sources / catchup / profile_is_default 반영 검증
python -c "
from src.notifier import build_briefing_message
b = {'headline_changes': ['x'], 'sangsang_ideas': []}
m = build_briefing_message(
    '2026-06-21', '2026-06-01', '2026-06-21', 5, b,
    mode='fallback', catchup=True,
    failed_sources=[{'source': 'Bad-RSS'}],
    profile_is_default=True,
    report_path='reports/2026-06-21/report.md',
)
print('fallback 안내:', 'AI 분석 실패' in m)      # True
print('catchup 안내:', 'catch-up' in m)           # True
print('프로필 부재:', '운영자 프로필 없음' in m)    # True
print('실패 소스:', 'Bad-RSS' in m)               # True
print('리포트 경로:', 'reports/2026-06-21' in m)  # True
"
```

### 구현 판단 / 특이사항
- Bash 차단으로 로컬 자동 실행 불가 — 수동 검증 스니펫 제공 (FEAT-06 섹션 9 기반)
- FEAT 문서 섹션 6 코드를 그대로 반영하되 docstring·공백 정리 (동작 동일)
- `send_discord`의 except는 `except Exception`으로 네트워크/타임아웃/연결거부 등 모든 예외를 포착해 파이프라인 중단 방지
- Webhook URL은 환경변수(DISCORD_WEBHOOK_URL)로만 주입 — 코드/문서에 URL 없음
- 분할 전송·파일 첨부·양방향 Bot 없음 (V0.2 금지 항목)

### 범위 준수 확인
- 수집/분석/리포트/파이프라인 로직 없음 (FEAT-03/04/05/07 선구현 없음)
- Webhook URL 코드 하드코딩 없음
- 분할 전송 없음 (2000자 초과는 절삭 마커로만 처리)
- 파이프라인 중단하는 예외 없음

### 제안 commit 메시지
```
feat(FEAT-06): Discord Webhook 전송기 구현 (notifier.py)

- src/notifier.py 신규: build_briefing_message(≤2000자 조립), send_discord(bool 반환)
- mode='fallback' / catchup / profile_is_default / failed_sources / report_path 메시지 반영
- 2000자 초과 시 절삭 마커 부착 (분할 전송 없음)
- webhook None/비2xx/예외 모두 False 반환, 파이프라인 중단 없음

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-09 — 주간보고 집계·생성 엔진 (`--weekly` 코어)
- 처리 이슈: #14
- 구현일: 2026-06-28
- 담당: builder (claude-sonnet-4-6)

### 생성/수정 파일 목록
| 파일 | 변경 | 설명 |
|------|------|------|
| `src/weekly.py` | 신규 | ISO 주차 산정·트리거/커서 판정, 정량 신호 집계(5종), 아이디어 통합, 상위기사 선별, run_weekly |
| `src/analyzer.py` | 수정(하단 추가) | synthesize_weekly, _build_weekly_prompt, _parse_weekly_response, _fallback_weekly 추가 (기존 일간 코드 무변경) |
| `src/reporter.py` | 수정(하단 추가) | save_bucket_report, build_weekly_report 추가 (기존 save_report 무변경) |
| `main.py` | 수정 | --weekly(store_true) + --week(YYYY-W##) 인자 추가 + run_weekly 디스패치 |

### 빌드/검증 결과
```
pytest -q tests/ → 11 passed in 1.62s  (기존 테스트 회귀 없음)

# fallback 생성 검증
python main.py --weekly --week 2026-W26 --no-discord --force-fallback
→ 2026-06-28 15:47:12 INFO 주간보고 생성: 2026-W26 → .../2026/weekly/W26.md (mode=fallback)
→ exit=0

# 섹션 존재 확인
지속줄기 표 OK / 강도추이 표 OK / 소스신호품질 표 OK / 공방픽 섹션 OK / 아이디어 통합 OK

# 멱등(skip) 검증
커서를 현재 주차(2026-W26)로 설정 후 --week 없이 실행
→ 2026-06-28 15:47:36 INFO 주간보고 skip — 이미 생성됨: 2026-W26
→ exit=0

# forced 재실행 검증
--week 2026-W26 명시 시 커서 무관 재생성
→ 주간보고 생성 로그 정상, exit=0
```

### 구현 판단/특이사항
- `tuple[int, int]` 타입 힌트는 Python 3.8 하위 호환성을 위해 `tuple`로 작성 (subscript tuple은 3.9+).
- `_build_weekly_prompt`에서 `import json as _json`으로 모듈 수준 json과 충돌 없이 사용.
- 기존 `_call_claude`를 그대로 재사용해 Anthropic SDK 중복 의존 없음.
- Discord 전송·launchd 주간 plist·tests/test_weekly.py는 FEAT-10 자리 — 절대 선구현하지 않음.
- `build_weekly_report`는 fallback 모드에서 헤더에 `> **AI 합성 실패 / 기본 주간 리포트**` 배지 삽입 (일간 패턴과 동형).

### 완료 조건 점검
- [x] `python main.py --weekly --no-discord --force-fallback` 실행 시 `reports/<ISO연도>/weekly/W##.md` 생성, exit=0
- [x] 같은 주에 `--weekly`(--week 없이) 재실행 시 skip (중복 생성 없음)
- [x] `--week 2026-W26` 지정 시 강제 생성(커서 일치여도 재생성)
- [x] 전문에 강도 추이·태그 지속성·소스 신호품질·공방 픽·일간 아이디어 통합 표/섹션 모두 존재
- [x] `aggregate_week`가 지속 줄기/단발 버스트/노이즈 소스/공방 픽 올바르게 분류
- [x] 기존 일간 QA(`pytest -q tests/test_pipeline.py`) 전부 통과 (회귀 없음)

### 제안 commit 메시지
```
feat(FEAT-09): 주간보고 집계·생성 엔진 구현 (#14)

- src/weekly.py 신규: ISO 주차 산정, 정량신호 집계(강도추이/태그지속성/소스품질/공방픽),
  일간 아이디어 통합, 상위기사 선별, run_weekly 오케스트레이션
- src/analyzer.py: synthesize_weekly + _build_weekly_prompt + _parse_weekly_response +
  _fallback_weekly 추가 (기존 일간 코드·_call_claude 무변경, 재사용)
- src/reporter.py: save_bucket_report(경로 일반화) + build_weekly_report(전문 11섹션) 추가
  (기존 save_report 무변경)
- main.py: --weekly/--week 인자 추가 + run_weekly 디스패치(일간보다 먼저)
- 기존 pytest 11 passed 회귀 없음. Discord/launchd/test_weekly.py는 FEAT-10 예정.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## FEAT-10 — 주간보고 Discord 다이제스트 + 일요일 스케줄러 + 통합테스트
- 처리 이슈: #15
- 구현일: 2026-06-28
- 담당: builder (claude-sonnet-4-6)

### 생성/수정 파일 목록
| 파일 | 변경 | 설명 |
|------|------|------|
| `src/notifier.py` | 수정 | `build_weekly_message` 추가 (9블록 plain content, ≤2000자) |
| `src/weekly.py` | 수정 | `send_discord` import 추가 + `run_weekly` FEAT-10 hook 자리에 Discord 전송 블록 연결 |
| `scripts/com.itsangsang.morningbrief.weekly.plist` | 신규 | 일요일(Weekday 0) 04:40 launchd 주간 잡 |
| `tests/test_weekly.py` | 신규 | 주간보고 통합 테스트 (7개 테스트, tmp_path 격리, --force-fallback hermetic) |
| `README.md` | 수정 | 주간 흐름 리포트(`--weekly`) 운영 섹션 추가 |

### 빌드/검증 결과
```
plutil -lint scripts/com.itsangsang.morningbrief.weekly.plist → OK

python3 -m pytest -q tests/
→ 18 passed in 1.73s  (기존 11 + 신규 7)

python main.py --weekly --week 2026-W26 --force-fallback --no-discord
→ 2026-06-28 16:29:17 INFO 주간보고 생성: 2026-W26 → reports/2026/weekly/W26.md (mode=fallback)
→ exit=0

build_weekly_message mock 빌드: length=625, ok=True (≤2000자, 9블록 순서 확인)
```

### 구현 판단/특이사항
- FEAT-10 스펙의 `_seed` 함수 내 `importance` 값 수정:
  스펙 원본(`9 if j == 0 else 2`)으로는 VentureBeat AI avg_importance ≈ 4.33 > NOISE_AVG_IMPORTANCE(3.0)
  → noise_candidate=False → `test_aggregate_signals` noise_sources 어서션 실패.
  의도("고볼륨·저중요")에 맞게 importance=2(고정)로 수정 → avg=2.0 < 3.0 → noise_candidate=True.
  relevance=8(j==0) 유지로 workshop_picks=7 어서션 유지.
- `from src.notifier import send_discord`를 모듈 상단에 추가하고, `build_weekly_message`는 hook 블록 안에서 lazy import로 재가져옴(FEAT-10 스펙 코드 그대로).
- 기존 일간 `build_briefing_message` 함수 완전 무변경.
- plist `/ABS/PATH/TO` 자리표시자 — README에 치환 절차 안내.

### 완료 조건 점검
- [x] `build_weekly_message` 출력 9블록, 길이 625자 ≤ 2000
- [x] `scripts/com.itsangsang.morningbrief.weekly.plist` `plutil -lint` OK, 일요일 04:40 확인
- [x] `pytest -q tests/test_weekly.py` 7 passed (신규)
- [x] `pytest -q tests/test_pipeline.py` 11 passed (회귀 없음)
- [x] `pytest -q tests/` 18 passed
- [x] `main.py --weekly --week 2026-W26 --force-fallback --no-discord` exit=0, W26.md 생성
- [x] README에 주간 plist 등록 + rsync 동기화 절차 포함
- [x] 기존 일간 plist · test_pipeline.py 수정 없음

### 제안 commit 메시지
```
feat(FEAT-10): 주간보고 Discord 다이제스트 + launchd 주간 잡 + 통합테스트 (#15)

- src/notifier.py: build_weekly_message 추가 (9블록 plain content, ≤2000자, 기존 send_discord 재사용)
- src/weekly.py: send_discord import + run_weekly FEAT-10 hook 자리에 Discord 전송 연결
  (--no-discord/실패 시 파이프라인 지속, 커서 갱신 유지)
- scripts/com.itsangsang.morningbrief.weekly.plist 신규: 일요일(Weekday 0) 04:40 launchd 주간 잡
- tests/test_weekly.py 신규: 7개 테스트 (ISO 주차 산정/parse/bounds/신호집계/빈주/리포트경로/커서 멱등)
  tmp_path 격리 + env override + --force-fallback hermetic (Claude API 미호출)
- README.md: 주간 흐름 리포트(--weekly) 운영 섹션 추가 (수동 생성/plist 등록/rsync 동기화)
- 기존 pytest 18 passed 전부 통과 (회귀 없음)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## 이슈 #13 (BUG) — 전 소스 수집 실패 시 catch-up 커서 보존

### 증상/근본원인
네트워크 단절로 전 소스 수집 실패(신규 0건)인 실행도 `last_success_at`(catch-up 커서)를 무조건 전진시켜, 단절 구간 기사가 다음 실행에서 영구 누락됨. 원인은 `pipeline.py` meta 갱신 블록이 결과와 무관하게 항상 `set_meta`를 호출한 것.

### 수정 내용
- `src/pipeline.py`:
  - `_should_advance_cursor(new_count, enabled_count, failed_count)` 순수 판정 함수 신규.
    - `all_sources_failed = (new_count==0) and (enabled_count - failed_count <= 0)` → 이때만 커서 보존.
    - ★ '정상 0건'(주말 등 소스 응답 OK·신규만 0건)과 구분: 성공 소스가 1개라도 있으면 전진.
  - meta 갱신 블록을 `if _should_advance_cursor(...)`로 가드. 미전진 시 `logging.warning`으로 보완 예정 안내.
  - `enabled_count = sum(...)`는 기존 `sources`(load_sources 전체 목록)로 계산 — 시그니처/콜렉터 무변경.
- `tests/test_pipeline.py`:
  - `ROOT` sys.path 주입 후 `_should_advance_cursor` import.
  - 회귀 테스트 5종 추가: 전소스실패→보존 / 정상0건→전진 / 부분실패→전진 / 신규有→전진 / happy-path 통합(정상 실행이 last_success_at 갱신).

### 빌드 결과
- `python3 -m pytest -q tests/test_pipeline.py` → **11 passed in ~1.5s** (기존 6 + 신규 5).

### 변경 파일
- `src/pipeline.py` (수정)
- `tests/test_pipeline.py` (수정)

### AC 충족
- [x] 전 소스 실패 실행 후 last_success_at 미갱신
- [x] 소스 정상 + 신규 0건은 기존대로 갱신
- [x] 커서 미전진 → 다음 정상 실행이 직전 last_success_at(장애 이전)부터 보완 수집
- [x] 회귀 테스트로 3+케이스 검증

### 제안 commit 메시지
```
fix(pipeline): 전 소스 수집 실패 시 catch-up 커서 미전진 (#13)

- _should_advance_cursor() 추가: 신규 0건 & 성공 소스 0개일 때만 last_success_at 보존
- 정상 0건(주말 등 소스 응답 OK)과 구분 — 단절 구간 기사 누락 방지
- 미전진 시 warning 로그로 다음 실행 보완 예정 안내
- tests: 회귀 5종 추가 (전소스실패/정상0건/부분실패/신규有/happy-path)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## FEAT-11 — 월간보고 신호·필터 엔진 (`--monthly` 코어 · #12 신뢰도/노이즈)
- 처리 이슈: #16
- 구현일: 2026-07-02
- 담당: builder (claude-sonnet-5)

### 문서 조회 특이사항
이 세션의 `gh` 명령이 샌드박스에서 전면 차단되어(읽기 전용 포함) 이슈 #16 본문을 직접 조회하지 못했다.
`03-설계/features/FEAT-11-월간보고-신호필터엔진.md`가 함수 시그니처·상수·docstring·검증 스크립트까지 포함해
그 자체로 완결된 구현 사양이었으므로, 이 문서 하나만으로 구현을 진행했다(공방장/이슈 정본은 이 문서를 가리킴).

### 생성 파일 목록
| 파일 | 설명 |
|------|------|
| `src/monthly.py` | 신규 — 월간 신호·필터 순수 함수 모듈 (I/O·Claude·네트워크 없음) |
| `tests/test_monthly.py` | 신규 — 이 FEAT의 순수 함수 단위 테스트 (엔드투엔드/Discord/스케줄러는 FEAT-13 몫, 미작성) |

### 구현 내용 (FEAT-11 문서 6장을 그대로 옮김 — 재작성 없음)
- 상수: `HIGH_IMPORTANCE`, `WORKSHOP_RELEVANCE`, `PERSIST_DAYS`(8), `BURST_MAX_DAYS`, `NOISE_MIN_COUNT`(15),
  `NOISE_AVG_IMPORTANCE`, `SOURCE_CAP_RATIO`(0.15), `TOP_LIMIT`(40), `FIRST_STAGE_KEYWORDS`,
  `FIRST_STAGE_MIN_SIGNAL`, `NOISE_TYPE_KEYWORDS`(7유형).
- `target_month(now)`: now 기준 직전 달 (year, month), 1월 실행 시 전년 12월로 롤오버.
- `parse_month_arg(s)`: `'YYYY-MM'` 파싱, 형식/범위 오류 시 ValueError.
- `month_bounds(y, m)`: `calendar.monthrange`로 1일·말일 date 반환.
- `month_key(y, m)`: 커서용 `'YYYY-MM'` 문자열.
- `partition_by_period(articles, first, last)`: published_at 우선(범위 내/외 분리), NULL/파싱불가는 collected_at 폴백.
- `first_stage_filter(articles)`: 키워드 화이트리스트 OR importance/relevance≥1 → (passed, rejected).
- `classify_noise(rejected)`: 탈락분을 7유형+기타로 건수 집계, `_total` 포함.
- `source_stats(articles)`: 소스별 건수·점유율·평균중요도·노이즈후보 여부(count desc 정렬).
- `cap_source_bias(articles, cap_ratio)`: 소스당 `max(3, round(N*0.15))` 상한, importance desc 상위만 유지.
- `aggregate_month(articles, first, last)`: 일자별 강도추이(전 일자 고정 길이)·태그((source,day) 고유조합 정규화 count)·지속줄기·버스트·소스통계·노이즈소스·공방픽·`cross_source` 반환.
- `select_top_articles_monthly(articles, limit=40)`: 제목정규화 중복제거 → 소스상한 → importance/relevance/교차출처수 정렬 상위 N, 각 항목에 `cross_source_count` 부가.
- `collect_monthly_ideas(reports_dir, first, last)`: `src/weekly.collect_daily_ideas`를 그대로 재사용(재구현 없음).
- 공통 헬퍼 `_day_of`, `_norm_title`, `collect_daily_ideas`는 전부 `from src.weekly import ...`로 **import 재사용만** — weekly 파일 자체는 1바이트도 수정하지 않음.

### 빌드/검증 결과
```
cd projects/AI-Morning-Brief/05-개발
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m pytest -q tests/
→ 38 passed in 1.88s   (기존 18(test_pipeline 11 + test_weekly 7) + 신규 test_monthly 20)

python3 -c "import src.monthly"  → 성공 (완료조건 1)
FEAT-11 문서 9장 검증 스크립트 실행 → 'OK' 출력, noise/aggregate 결과 기대값과 일치
```
기존 `tests/test_pipeline.py`(11) · `tests/test_weekly.py`(7) 전부 그대로 통과 — 회귀 없음.

### 구현 판단 / 특이사항 — FEAT 문서 서술과 코드의 사소한 불일치 1건
FEAT-11 문서 7장(예외 처리)은 "빈 월(기사 0건)일 때 `peak_day=None`"이라고 서술하지만,
문서 6-5절에 명시된 `aggregate_month` 코드를 그대로 옮기면 `daily_intensity`가 항상
그 달의 전체 일수만큼(count=0 포함) 채워지므로 `max(daily, key=...)`가 `daily`가 비어있지
않은 한 dict를 반환한다(월 자체가 0일이 아닌 이상 None이 될 수 없음).
→ 코드는 6장 명세 그대로 두고(임의 수정 안 함), 테스트만 실제 동작(`peak_day["count"] == 0`)에
맞춰 작성. (필요 시 공방장/설계자에게 이 문서 불일치를 별도 보고할 수 있음 — 이번 이슈 범위 밖이라 코드 변경은 하지 않았음.)

### 범위 준수 확인
- `run_monthly`/`synthesize_monthly`/`build_monthly_report`/`main.py --monthly` 분기 없음 (FEAT-12).
- `build_monthly_message`/월간 plist/README/엔드투엔드 통합테스트 없음 (FEAT-13).
- `weekly.py`(`aggregate_week`/`run_weekly`) 및 `analyzer.py`/`pipeline.py` 등 기존 파일 무수정.
- DB 스키마 변경 없음, 파일·DB 쓰기·Claude 호출·네트워크 접근 없음 (순수 계산 계층).
- 신규 외부 라이브러리 추가 없음(표준 라이브러리 `calendar`/`datetime`/`re`/`collections`만 사용).
- git/gh 명령 실행 없음(commit/push는 GitHub 관리자 몫).

### 제안 commit 메시지
```
feat(FEAT-11): 월간보고 신호·필터 엔진 구현 (#12 코어, 이슈 #16)

- src/monthly.py 신규: 기간경계분리/1차관련성필터/노이즈분류/소스편중정규화/
  집계/대표기사선별 — 순수 함수·상수만(I/O·Claude·네트워크 없음)
- weekly._day_of/_norm_title/collect_daily_ideas는 import 재사용만(재구현 없음)
- tests/test_monthly.py 신규: 순수 함수 단위 테스트 20개
- 기존 pytest 38 passed 전부 통과 (weekly/pipeline 회귀 없음)
- run_monthly·Claude합성·Discord·스케줄러는 FEAT-12/13에서 이어감

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

---

## FEAT-12 — 월간보고 합성·전문 생성·오케스트레이션 (`--monthly` 엔진 · 신뢰도 라벨)
- 처리 이슈: #17 (매칭 FEAT: `FEAT-12-월간보고-합성-전문생성-오케스트레이션.md`, FEAT 본문 표기상 매칭 이슈는 #12이나 실제 4단계 등록 이슈 번호는 #17 — 지시서 기준)
- 구현일: 2026-07-02
- 담당: builder (claude-sonnet-4-6 계열, Sonnet 5)

### 문서 조회 특이사항
이 세션에서도 `gh` 명령이 차단될 가능성이 있어 이슈 본문 대신 지시서가 지정한 대로
`03-설계/features/FEAT-12-월간보고-합성-전문생성-오케스트레이션.md`를 사양 정본으로 삼아 진행했다
(실제로는 `gh` 호출을 시도하지 않고 처음부터 FEAT 문서만 읽었다 — 지시서에 이미 "gh 차단 가능성 → FEAT 문서를 정본으로" 라고 명시되어 있었기 때문).
선행 이슈 #16(FEAT-11) 산출물 `src/monthly.py`의 순수 함수는 읽기만 하고 재구현하지 않았다.

### 변경 파일 목록
| 파일 | 변경 내용 |
|------|-----------|
| `src/monthly.py` | (수정) 하단에 `run_monthly(args)` 오케스트레이션 추가. FEAT-11 순수 함수 import 소비만, 기존 함수 무변경 |
| `src/analyzer.py` | (수정) 하단에 `synthesize_monthly` / `_build_monthly_prompt` / `_parse_monthly_response` / `_label_by_cross_source` / `_fallback_monthly` 5개 함수 추가. 기존 `analyze`/`synthesize_weekly`/`_call_claude`/`_fallback_analyze` 등 무변경, `_call_claude`만 재사용 |
| `src/reporter.py` | (수정) 하단에 `_count_labels` + `build_monthly_report` 추가. `save_bucket_report`는 재사용만(수정 없음), 기존 `build_report`/`build_weekly_report`/`save_report` 무변경 |
| `main.py` | (수정) `--monthly`(store_true) / `--month YYYY-MM` 인자 추가 + `--weekly` 분기 다음·일간 파이프라인 분기 앞에 `run_monthly` 디스패치 추가. 기존 옵션·분기 순서 무변경 |

### 구현 내용 (FEAT-12 문서 6장을 그대로 반영)
- `run_monthly(args)`: `load_config/load_secrets/load_operator_profile → init_db → 대상월 산정(--month면 parse_month_arg+forced=True, 아니면 target_month(now)) → 커서(last_monthly_ym) 멱등 skip → month_bounds → get_articles_by_range(collected_at 넓은 범위) → partition_by_period(#12-1) → first_stage_filter(#12-3) → classify_noise(#12-7) → aggregate_month/select_top_articles_monthly/collect_monthly_ideas → basis dict 구성 → synthesize_monthly → build_monthly_report → save_bucket_report(bucket="monthly", name="M##") → set_meta(last_monthly_ym) → return 0`. FEAT-13 Discord hook 자리는 주석으로 비움.
- `synthesize_monthly(...)`: `force_fallback`/`api_key` 없음 → 즉시 `_fallback_monthly`. 아니면 `_build_monthly_prompt → _call_claude(기존 재사용) → _parse_monthly_response`, 모든 예외를 `_fallback_monthly`로 흡수(mode="fallback").
- `_build_monthly_prompt(...)`: 정량신호(persistent_stems/bursts/sources_top/noise_sources)+대표기사(cross_source_count 동봉)+분석기반(basis)+아이디어를 system/user에 투입, 신뢰도 라벨 규칙([확정]=교차출처≥2 또는 공식 출처, [추정]=단일출처·해석, [주의]=미확인·논란)과 2차 필터(공방 적용성) 지시를 system에 명시.
- `_parse_monthly_response(text)`: `{`~`}` 슬라이스 후 `json.loads`, 누락 필드(`one_line_summary`/`flow_themes`/`notable_events`/`workshop_actions`/`next_month_watch`/`narrative`) 기본값 보정.
- `_label_by_cross_source(article)`: fallback 전용 — `cross_source_count>=2`면 `[확정]`, 아니면 `[추정]` 기계 부여.
- `_fallback_monthly(...)`: 규칙기반 합성, `flow_themes`/`notable_events`는 대표기사 제목 앞에 `_label_by_cross_source` 라벨 접두, `narrative`는 `[확정] [기본 월간 리포트] ...` + 노이즈/범위외 건수 부기.
- `build_monthly_report(...)`: FEAT 문서가 지정한 13개 섹션(0~12) 순서 그대로 구현 — 헤더+분석기반 한 줄(`📊 수집·1차통과·대표·범위외·노이즈·신뢰도[확정/추정/주의]` 카운트, `_count_labels`로 flow_themes+notable_events+narrative 텍스트에서 라벨 문자열 등장수 집계) → `## 0` 노이즈 유형별 표+범위외 건수 → `## 1` 한줄요약 → `## 2` 흐름테마 → `## 3` 주목사건 → `## 4` 강도추이표(⚠️ 데이터빈약 비고) → `## 5` 지속줄기/버스트 태그표 → `## 6` 소스 편중도표(점유율%=share×100 반올림, 노이즈⚠️) → `## 7` 공방픽+즉시착수액션 → `## 8` 일간아이디어통합 → `## 9` 다음달관전포인트 → `## 10` 월간서술 → `## 11` 대표원문링크(top20, 교차출처수 동봉) → `## 12` 범위외 참고항목(제목·published_at, 상한 20건 + 초과분 안내).
- `main.py`: `--monthly`/`--month` argparse 추가, `if getattr(args, "monthly", False): from src.monthly import run_monthly; return run_monthly(args)` 를 `--weekly` 분기 다음·일간 `pipeline.run` try 블록 앞에 배치.

### 빌드/검증 결과
```
cd projects/AI-Morning-Brief/05-개발
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m pytest -q tests/
→ 38 passed in 1.67s   (기존 38개 그대로 — 신규 회귀 없음. tests/test_monthly.py 엔드투엔드 파트는 FEAT-13 몫이라 추가하지 않음)
```

FEAT-12 문서 9장 수동 검증 스크립트(6월 28일치 시드 + `--month 2026-06 --no-discord --force-fallback`)를 격리 DB/REPORTS_DIR(env override)로 실행:
```
python main.py --monthly --month 2026-06 --no-discord --force-fallback
→ exit=0, "월간보고 생성: 2026-06 → .../2026/monthly/M06.md (mode=fallback)"
grep "분석 기반|수집" M06.md      → 기반 명시 OK  (#12-7)
grep "\[확정\]|\[추정\]" M06.md    → 신뢰도 라벨 OK (#12-2)
grep "점유율|편중" M06.md          → 소스 편중 표 OK (#12-5,6)
grep "범위 외" M06.md              → 범위 외 항목 OK (#12-1)
```
추가로 직접 확인:
- `--month` 강제 지정 시 커서(`last_monthly_ym`)가 같아도 재생성됨(forced=True가 skip 체크를 우회) — 2회 연속 실행 모두 정상 생성.
- 커서를 오늘(2026-07-02) 기준 `target_month(now)`와 동일한 `"2026-06"`으로 미리 세팅한 뒤 `--month` 없이 실행 → 로그 `"월간보고 skip — 이미 생성됨: 2026-06"` + 리포트 파일 미생성(멱등 확인).

### 구현 판단 / 특이사항
- `src/monthly.py` 하단에 `run_monthly`가 `src.analyzer`/`src.reporter`를 모듈 레벨에서 import한다. `analyzer.py`/`reporter.py`는 `monthly.py`나 `weekly.py`를 import하지 않으므로 순환 참조 없음(기존 `weekly.py`가 이미 동일 패턴으로 `analyzer`/`reporter`를 import하고 있어 동형).
- `run_monthly` 블록의 `logging` 사용은 파일 상단 `import logging`이 이미 있어 별도 별칭(`_logging`) 없이도 됐지만, FEAT-11이 이미 확정한 상단 import 라인을 건드리지 않기 위해(회귀 방지 원칙) 오케스트레이션 섹션 전용으로 `import logging as _logging`을 추가해 사용 — 동작은 표준 `logging`과 동일.
- `build_monthly_report`는 FEAT 문서에서 `...`(builder 구현 위임)로 남겨진 함수라 표 포맷은 FEAT 문서의 힌트 주석(예: `📊 수집 287 · 1차통과 188 ...` 예시 문자열, 표 컬럼 순서)을 그대로 따랐다. `weekly.py`의 `build_weekly_report` 섹션 구성을 패턴으로 재사용했다(신규 로직 최소화).

### 범위 준수 확인
- FEAT-11 순수 함수(`aggregate_month`/`first_stage_filter`/`classify_noise`/`source_stats`/`cap_source_bias`/`select_top_articles_monthly`/`partition_by_period`/`target_month`/`parse_month_arg`/`month_bounds`/`month_key`/`collect_monthly_ideas`) — import 소비만, 재구현·수정 없음.
- 기존 일간·주간 코드(`analyze`/`synthesize_weekly`/`build_report`/`build_weekly_report`/`run_weekly`/`pipeline.run`) 무수정 — 하단 추가만.
- `notifier.build_monthly_message`/월간 Discord 전송/월간 plist/`tests/test_monthly.py` 엔드투엔드/README 갱신 없음 (전부 FEAT-13).
- DB 스키마 변경 없음 — 커서는 기존 `meta` key-value row(`last_monthly_ym`)만 사용, 새 컬럼/테이블 없음.
- `save_bucket_report` 수정 없음, 새 저장 헬퍼 추가 없음 — 재사용만.
- 신규 외부 라이브러리 추가 없음.
- git/gh 명령 실행 없음(commit/push/이슈 close는 GitHub 관리자 몫).

### 제안 commit 메시지
```
feat(FEAT-12): 월간보고 합성·전문생성·오케스트레이션 구현 (이슈 #17)

- src/monthly.py: run_monthly(args) 오케스트레이션 추가
  (트리거 멱등 → 경계분리 → 1차필터 → 노이즈분류 → 집계 → 대표선별 →
   아이디어 → Claude합성 → 전문저장 → 커서갱신). FEAT-11 순수 함수는 소비만.
- src/analyzer.py: synthesize_monthly/_build_monthly_prompt/
  _parse_monthly_response/_label_by_cross_source/_fallback_monthly 추가
  (신뢰도 라벨 [확정]/[추정]/[주의], _call_claude 재사용)
- src/reporter.py: build_monthly_report 추가 (13개 섹션, #12-7 노이즈·분석기반
  상단 명시, #12-5,6 소스 편중도 표, #12-1 범위 외 참고 항목). save_bucket_report 재사용.
- main.py: --monthly/--month 인자 + run_monthly 디스패치 (--weekly 다음, 일간 앞)
- 기존 pytest 38 passed 그대로 통과(회귀 없음). FEAT-12 수동 검증 스크립트 전항목 통과
  (분석기반/신뢰도라벨/소스편중/범위외 항목 OK, --month 강제 재생성·커서 skip 확인)
- Discord 전송/월간 plist/엔드투엔드 테스트는 FEAT-13 범위로 제외

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

---

## FEAT-13 — 월간보고 Discord 다이제스트 + 매월1일 스케줄러 + 통합테스트
- 처리 이슈: #18 (매칭 FEAT: `FEAT-13-월간보고-Discord다이제스트-스케줄러-통합테스트.md`)
- 구현일: 2026-07-02
- 담당: builder (Sonnet 5)

### 문서 조회 특이사항
`gh issue view 18` 시도 시 `PreToolUse:Bash hook error — 이 에이전트는 GitHub 쓰기 권한이 없습니다`로 차단됨(지시서에 명시된 대로 예상된 상황). FEAT-13 문서를 사양 정본으로 삼아 진행했다. 선행 이슈 #16(FEAT-11)·#17(FEAT-12) 산출물(`src/monthly.py`의 `run_monthly`, `src/notifier.py`의 `build_weekly_message`/`send_discord` 패턴, `main.py` `--monthly`/`--month` 분기)은 읽기만 하고 소비만 했다(재구현 없음).

### 변경 파일 목록
| 파일 | 변경 내용 |
|------|-----------|
| `src/notifier.py` | (수정) 하단(`send_discord` 바로 앞)에 `build_monthly_message(...)` 추가. 기존 `build_briefing_message`/`build_weekly_message`/`send_discord` 무변경 |
| `src/monthly.py` | (수정) 상단 오케스트레이션 import 블록에 `from src.notifier import send_discord` 추가 + `run_monthly`의 "(FEAT-13 hook)" 자리에 Discord 전송 블록 연결. FEAT-11/12 로직·시그니처 무변경 |
| `scripts/com.itsangsang.morningbrief.monthly.plist` | 신규 — 매월 1일(Day=1) 04:50 `main.py --monthly` 실행 launchd 템플릿(주간 plist와 동형 패턴, `/ABS/PATH/TO` 자리표시자) |
| `tests/test_monthly.py` | (수정) `env` autouse fixture(tmp_path 경로 override) 추가 + 엔드투엔드/Discord hook/다이제스트 길이 통합테스트 6개 추가 |
| `README.md` | (수정) "핵심 기능" 표에 월간 흐름 리포트 행 추가 + 실행 방법 코드블록에 `python main.py --monthly` 추가 + "월간 흐름 리포트(--monthly) 운영" 섹션 신규 |

### 구현 내용 (FEAT-13 문서 6장을 그대로 반영)
- `build_monthly_message(ym, first, last, signals, basis, synthesis, report_path, char_limit=2000)`: FEAT 문서 6-1 코드를 그대로 옮김 — 9블록(월+한줄요약 / 분석기반+신뢰도카운트 / 흐름테마Top4 / 주목사건Top3 / 지속줄기Top6 / 편중·노이즈 / 공방액션Top3 / 다음달관전 / 리포트경로), `re.findall`로 flow_themes+notable_events+narrative 텍스트에서 `[확정]/[추정]/[주의]` 라벨 등장수 집계, 2000자 초과 시 안전망 절단.
- `run_monthly` hook: `save_bucket_report` 직후·`set_meta` 직전에 `if not getattr(args, "no_discord", False): from src.notifier import build_monthly_message; msg = build_monthly_message(...); send_discord(secrets.get("DISCORD_WEBHOOK_URL"), msg)` 삽입. `send_discord`는 상단 import 재사용(기존 정책대로 실패해도 로그만, 파이프라인 지속).
- `scripts/com.itsangsang.morningbrief.monthly.plist`: 주간 plist(`com.itsangsang.morningbrief.weekly.plist`)와 동일 패턴(`/ABS/PATH/TO/main.py --monthly`, `WorkingDirectory /ABS/PATH/TO`) — `StartCalendarInterval`에 `Day=1, Hour=4, Minute=50`.
- `tests/test_monthly.py`: `test_weekly.py`와 동형의 `env(tmp_path, monkeypatch)` autouse fixture(`MORNINGBRIEF_DB/RAW_DIR/REPORTS_DIR`) 추가. 신규 테스트 6개 — `test_report_created_and_sections`(전문 생성·섹션), `test_cursor_idempotent`(멱등 커서 + 재실행 skip), `test_monthly_run_no_discord_skips_send`(in-process, `--no-discord`→`send_discord` 미호출), `test_run_monthly_calls_discord_hook_when_enabled`(in-process, hook이 `build_monthly_message` 결과를 `send_discord`로 전달하는지), `test_digest_length`/`test_digest_length_dense_synthesis_still_fits`(다이제스트 길이 ≤2000).
- README: FEAT 6-5절 운영 섹션 내용을 반영해 "월간 흐름 리포트(--monthly) 운영" 신규 섹션(수동생성/launchd 등록/rsync 동기화/산출물/실행순서) 추가.

### 빌드/검증 결과
```
cd projects/AI-Morning-Brief/05-개발
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m pytest -q tests/
→ 44 passed in 2.40~2.50s   (기존 38 + 신규 6, 전부 통과 — 회귀 없음)

/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m pytest -q tests/test_weekly.py tests/test_pipeline.py
→ 18 passed   (기존 weekly/daily 회귀 없음 재확인)

plutil -lint scripts/com.itsangsang.morningbrief.monthly.plist
→ OK

python3 -c "... build_monthly_message(...) ..."   (FEAT 문서 9장 M06 mock 그대로)
→ 9블록 렌더 결과가 FEAT 문서 6-2 예시와 동일한 형태로 출력, len=392 (<=2000) ok=True
```

### 구현 판단 / 특이사항
- FEAT 6-4의 예시 테스트 중 `test_target_month_and_parse`/`test_period_and_two_stage_filter`/`test_source_bias_cap_and_top`는 FEAT-11이 이미 순수 함수 단위 테스트로 동일 로직을 촘촘히 커버하고 있어(예: `test_target_month_normal_and_year_rollover`, `test_partition_by_period_in_out_and_fallback`, `test_cap_source_bias_applies_cap`) 이름만 바꿔 중복 추가하지 않았다. 대신 FEAT-13 완료조건(8장)이 요구하는 신규 검증 영역(전문 섹션·멱등 커서·다이제스트 길이·Discord hook 연결)만 추가해 테스트 중복을 줄였다 — 완료조건 문항은 모두 커버됨.
- `test_monthly_run_no_discord_skips_send`/`test_run_monthly_calls_discord_hook_when_enabled`는 FEAT 문서 예시(subprocess 기반)보다 한 단계 더 나아가 **in-process**로 `src.monthly.send_discord`를 monkeypatch해 hook이 실제로 `build_monthly_message` 결과를 전달하는지 직접 검증했다(subprocess로는 프로세스 경계 때문에 monkeypatch가 전달되지 않아 검증력이 약함). `run_monthly`가 `cfg=load_config()`로 상대경로 `config/config.yaml`을 읽으므로 이 두 테스트는 반드시 `05-개발/` cwd에서 실행되어야 한다(기존 관례와 동일, `pytest -q tests/`를 `05-개발/`에서 실행하면 문제 없음).
- `run_monthly`의 오케스트레이션 import 블록(`from src.analyzer import synthesize_monthly` 등) 바로 아래에 `from src.notifier import send_discord`를 추가했다 — FEAT 문서가 명시한 "monthly.py 상단 import 추가" 위치(오케스트레이션 섹션)를 그대로 따름.

### 범위 준수 확인
- 월간 집계·필터·편중·대표선별(FEAT-11)·합성·전문빌더·`run_monthly` 오케스트레이션 본체(FEAT-12) 로직 추가/수정 없음 — 소비만.
- 임베드/분할 전송 없음 — `send_discord`에 plain content 단일 문자열만 전달(기존 `send_discord` 시그니처·구현 무변경).
- 일간·주간 plist(`com.itsangsang.morningbrief.plist`/`.weekly.plist`)·테스트(`tests/test_pipeline.py`/`tests/test_weekly.py`) 무수정 — 별도 실행으로 회귀 없음 재확인.
- DB 스키마 변경 없음 — 커서는 FEAT-12가 둔 `meta.last_monthly_ym` 그대로 사용.
- 신규 외부 라이브러리 추가 없음(표준 라이브러리 `re`만 `build_monthly_message` 내부에서 사용, 기존 `notifier.py` 의존성 그대로).
- git/gh 쓰기 시도 없음(최초 `gh issue view` 1회는 차단 확인 목적, 이후 재시도 없음). commit/push/이슈 close는 GitHub 관리자 몫.

### 제안 commit 메시지
```
feat(FEAT-13): 월간보고 Discord 다이제스트 + 매월1일 스케줄러 + 통합테스트 (이슈 #18)

- src/notifier.py: build_monthly_message 추가 (9블록, plain content, 2000자 이내,
  분석기반·신뢰도카운트[확정/추정/주의] 포함). 기존 함수 무변경.
- src/monthly.py: run_monthly의 FEAT-13 hook 자리에 Discord 전송 연결
  (--no-discord면 생략, 실패해도 로그만·파이프라인 지속). send_discord import 추가.
- scripts/com.itsangsang.morningbrief.monthly.plist 신규: 매월 1일 04:50 실행
  (일간 04:30·주간 04:40 이후 배치로 DB 경합 최소화).
- tests/test_monthly.py: env autouse fixture + 엔드투엔드/Discord hook/
  다이제스트 길이 통합테스트 6개 추가 (hermetic, --force-fallback).
- README.md: 월간 흐름 리포트 기능 소개 + 운영(launchd 등록·rsync 동기화) 섹션 추가.
- pytest 44 passed (기존 38 + 신규 6, 회귀 없음). weekly/pipeline 18 passed 별도 재확인.
- 월간보고(#12) 3/3 FEAT 완료 — --monthly 증분이 배달·예약·검증까지 마감.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

---

## #20 — 월간/주간 Discord 메시지 절삭 시 전체 리포트 링크 유실 수정
- 처리 이슈: #20 (버그, FEAT 미대응 — `type:bug`)
- 구현일: 2026-07-02
- 담당: builder (Sonnet 5)

### 문서 조회 특이사항
FEAT 매칭 없음(버그 이슈) — 최소 문서 원칙에 따라 루트 `CLAUDE.md` + `gh issue view 20 -R LeeSiHyeon711/AI-Morning-Brief` 본문만 읽고 진행. 이슈 본문을 구현 스펙으로 그대로 반영했다.

### 변경 파일 목록
| 파일 | 변경 내용 |
|------|-----------|
| `src/notifier.py` | (수정) 신규 헬퍼 `_finalize_with_report_path(body_lines, report_path, char_limit)` 추가 + `build_weekly_message`/`build_monthly_message`의 안전망 절삭 로직(`msg[:char_limit-1]+"…"`)을 이 헬퍼 호출로 교체 |

### 원인 (이슈 본문 그대로)
`build_weekly_message`(구 185~186행)·`build_monthly_message`(구 257~258행)의 안전망이 블록 순서와 무관하게 조립된 문자열 뒤를 단순 슬라이싱해, 마지막 블록인 `📄 전체 리포트: {report_path}` 줄이 절삭 시 통째로 사라졌다.

### 수정 내용
- `_finalize_with_report_path(body_lines, report_path, char_limit)` 신규: `📄 전체 리포트: {report_path}` 줄을 별도로 조립해 길이를 먼저 확보해두고, 나머지 블록 본문(`body_lines`)을 `char_limit - (report_line 길이 + truncate_marker 길이 + 개행 2개)` 이내로 자른 뒤 `truncate_marker`("…(이하 생략, 전체는 로컬 리포트 참조)" — `build_briefing_message`와 동일 문구 재사용) → report_path 줄 순으로 항상 이어붙인다. 절삭이 없으면 마커 없이 본문+report_path만 합친다.
- `build_weekly_message`/`build_monthly_message` 양쪽 모두 마지막 블록(9번, 전체 리포트 경로) 조립 부분을 `return _finalize_with_report_path(L, report_path, char_limit)` 한 줄로 교체. 블록 1~8 조립 로직은 무변경.

### 빌드/검증 결과
```
cd projects/AI-Morning-Brief/05-개발
python3 -m pytest -q tests/
→ 44 passed in ~2.9s (기존 전부 통과, 회귀 없음 — 특히 test_monthly.py의
   test_digest_length_dense_synthesis_still_fits가 절삭 경계값 케이스를 검증)

# 실제 reports/2026/monthly/M06.md 내용을 그대로 옮긴 긴 synthesis로 시뮬레이션
python3 scratchpad/simulate_issue20.py
→ MONTHLY: length=2000, contains report line=True, ends with report line=True,
   contains truncate marker=True
→ WEEKLY:  length=2000, contains report line=True, ends with report line=True,
   contains truncate marker=True
(스크립트는 05-개발/ 폴더 밖 세션 스크래치패드에서 실행 — 05-개발/에는 남기지 않음)
```

### 구현 판단 / 특이사항
- 두 함수가 동일한 절삭 결함을 공유하고 있어 이슈 수정 방향 3번("양쪽 모두 동일한 방식으로 고친다")을 그대로 따르려면 로직 공유가 자연스러워, 작은 private 헬퍼 하나로 통합했다(신규 파일·리팩터링 확장 없음, 스코프 내 최소 변경).
- `truncate_marker`는 이슈 본문이 예시로 든 `build_briefing_message`의 `"…(이하 생략, 전체는 로컬 리포트 참조)"` 문구를 그대로 재사용(신규 문구 발명 없음).
- report_path 자체가 비정상적으로 길어 `char_limit - reserved`가 음수가 되는 극단값은 `max(..., 0)`으로 방어(빈 본문 + 마커 + report_path만 남는 형태로 안전하게 수렴).

### 범위 준수 확인
- 이슈 #20 범위(주간·월간 안전망 절삭 로직) 밖 수정 없음 — 블록 1~8 조립·`build_briefing_message`·`send_discord`·#21 관련 프롬프트 로직 무변경.
- 신규 외부 라이브러리 추가 없음.
- git/gh 쓰기 시도 없음(`gh issue view`만 실행). commit/push/이슈 close는 GitHub 관리자 몫.

### 제안 commit 메시지
```
fix(#20): 월간/주간 Discord 메시지 절삭 시 전체 리포트 링크 유실 수정

- src/notifier.py: _finalize_with_report_path 헬퍼 추가 — report_path 줄 길이를
  먼저 확보해두고 나머지 본문만 char_limit 이내로 자른 뒤 절삭 안내 마커
  ("…(이하 생략, 전체는 로컬 리포트 참조)") + report_path 줄을 항상 끝에 보존
- build_weekly_message/build_monthly_message 둘 다 동일 헬퍼로 안전망 교체
  (구 "msg[:char_limit-1]+…" 무조건 슬라이싱 → report_path 줄 유실 버그 수정)
- pytest 44 passed(회귀 없음). M06.md 실데이터 기반 시뮬레이션으로 절삭 시에도
  '📄 전체 리포트' 줄이 항상 남는 것 확인

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

---

## #21 — 월간/주간 합성 프롬프트에 항목별 글자수 상한 지시 추가
- 처리 이슈: #21 (버그, FEAT 미대응 — `type:bug`)
- 구현일: 2026-07-02
- 담당: builder (Sonnet 5)

### 문서 조회 특이사항
FEAT 매칭 없음(버그 이슈) — 최소 문서 원칙에 따라 루트 `CLAUDE.md` + `gh issue view 21 -R LeeSiHyeon711/AI-Morning-Brief` 본문만 읽고 진행. 이슈 본문을 구현 스펙으로 그대로 반영했다. (이슈 #20의 근본 원인 이슈이나, #20에서 이미 고친 `notifier.py`의 절삭 안전망은 건드리지 않았다.)

### 변경 파일 목록
| 파일 | 변경 내용 |
|------|-----------|
| `src/analyzer.py` | (수정) `_build_weekly_prompt`(구 191~197행)·`_build_monthly_prompt`(구 296~301행)의 출력 스키마 지시문에서 `flow_themes`/`notable_events`/`workshop_actions`/`next_week_watch`(또는 `next_month_watch`) 각 항목에 글자수 상한 문구 추가 |

### 원인 (이슈 본문 그대로)
`synthesize_monthly()`·`synthesize_weekly()`의 프롬프트가 항목 **개수**만 제한하고(`최대 4개` 등) 항목 하나당 **글자수는 전혀 제한하지 않아**, Discord 2000자 안전망(#20)에 걸리는 절삭이 쉽게 발생했다. 같은 파일 93번째 줄 부근(`_build_prompt`)은 `"<1800자 이내 한국어>"`처럼 명시적 글자수 지시를 이미 포함하는데, 월간·주간 합성 프롬프트에는 이 지시가 빠져 있었다.

### 수정 내용
- `_build_weekly_prompt`: `flow_themes`/`next_week_watch`에 "각 항목 80자 이내", `notable_events`/`workshop_actions`에 "각 항목 100자 이내" 문구를 기존 개수 제한 문구 뒤에 이어 붙였다. 기존 문구·스타일(배열 안 문자열 하나로 스키마 설명을 겸하는 방식)은 그대로 유지.
- `_build_monthly_prompt`: `flow_themes`/`notable_events`는 `[라벨]` 접두가 붙으므로 이슈 본문의 제안대로 "각 항목 100자 이내([라벨] 포함)"로 라벨 포함 여부를 명시. `workshop_actions`는 "각 항목 100자 이내", `next_month_watch`는 "각 항목 80자 이내" 추가.
- LLM 출력 글자수를 코드로 강제하는 로직(예: 파싱 후 truncate)은 이슈 범위 밖이라 추가하지 않았다 — `_parse_weekly_response`/`_parse_monthly_response`는 무변경. #20의 Discord 절삭 안전망이 최종 방어선 역할을 그대로 유지한다.

### 빌드/검증 결과
```
cd projects/AI-Morning-Brief/05-개발
python3 -m pytest -q
→ 44 passed in 2.40s (기존 전부 통과, 회귀 없음)
```
프롬프트 문자열 자체를 assert하는 테스트는 없어(grep 확인) 테스트 문구 갱신은 불필요했다.

### 구현 판단 / 특이사항
- 글자수 상한 값(주간·월간 모두 80/100자 두 단계, 월간은 라벨 포함 명시)은 이슈 본문의 예시("각 항목 100자 이내, [라벨] 포함")를 기준으로 항목 성격(제목형 vs 서술형)에 맞춰 나눴다 — 이슈가 특정 숫자를 강제하지 않았고 "권고 지시 추가"가 완료 조건이므로 과설계 없이 기존 문구 스타일에 자연스럽게 맞췄다.
- `narrative` 필드는 이슈 본문이 명시적으로 언급한 4개 배열 항목(flow_themes/notable_events/workshop_actions/next_*_watch)에 포함되지 않아 손대지 않았다(월간 narrative는 이미 "핵심 주장마다 [라벨] 접두" 지시가 있고, 이슈는 배열 항목의 개수-only 문제만 지적).

### 범위 준수 확인
- 이슈 #21 범위(월간·주간 합성 프롬프트 문자열) 밖 수정 없음 — `notifier.py`(#20 안전망) 재수정 없음, `_parse_weekly_response`/`_parse_monthly_response`/fallback 함수/일간 `_build_prompt` 무변경.
- LLM 출력 글자수를 코드로 강제하는 로직 추가 없음(이슈 본문이 명시적으로 범위 제외).
- git/gh 쓰기 시도 없음(`gh issue view`만 실행). commit/push/이슈 close는 GitHub 관리자 몫.

### 제안 commit 메시지
```
fix(#21): 월간/주간 합성 프롬프트에 항목별 글자수 상한 지시 추가

- src/analyzer.py: _build_weekly_prompt/_build_monthly_prompt의 출력 스키마
  지시문에 flow_themes/notable_events/workshop_actions/next_week_watch·
  next_month_watch 각 항목 글자수 상한(80~100자, 월간은 [라벨] 포함 명시) 추가
- 기존 개수 제한("최대 N개") 문구는 유지, 글자수 지시만 자연스럽게 이어붙임
- 코드로 글자수를 강제하는 로직은 이슈 범위 밖 — #20의 Discord 절삭 안전망이
  최종 방어선 역할 유지
- pytest 44 passed(회귀 없음)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```
